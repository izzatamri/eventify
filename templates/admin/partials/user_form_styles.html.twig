<style>
	.admin-user-form-page { background: var(--ztc-bg-bg-1, #fff); min-height: 100vh; }
	.admin-form-layout { display: flex; gap: 0; max-width: 1400px; margin: 0 auto; padding: 0 1.5rem 2rem; }
	.admin-form-sidebar {
		width: 280px; flex-shrink: 0; position: sticky; top: 1.5rem; align-self: start;
		border-right: 1px solid rgba(0,0,0,.08); padding-right: 1.5rem; margin-right: 2rem;
	}
	.admin-form-main { flex: 1; min-width: 0; padding-bottom: 100px; }
	@media (max-width: 991px) {
		.admin-form-layout { flex-direction: column; padding: 0 1rem 2rem; }
		.admin-form-sidebar { width: 100%; position: static; border-right: none; padding-right: 0; margin-right: 0; border-bottom: 1px solid rgba(0,0,0,.08); padding-bottom: 1.5rem; margin-bottom: 1rem; }
		.admin-form-sticky-save { left: 1rem; right: 1rem; bottom: 1rem; }
	}
	.admin-user-form-page .back-link {
		display: block; margin-bottom: 1.25rem; color: var(--ztc-bg-bg-9, #2E4DFD); text-decoration: none; font-size: 0.9375rem; font-weight: 600;
		transition: color 0.3s ease;
	}
	.admin-user-form-page .back-link:hover { color: var(--ztc-bg-bg-8, #002466); }
	.admin-user-form-page .summary-card {
		background: var(--ztc-bg-bg-1, #fff); border-radius: 12px;
		box-shadow: 0 2px 8px rgba(0,0,0,.06); padding: 1.25rem; position: relative; overflow: hidden;
		border: 1px solid rgba(0,0,0,.04);
	}
	.admin-user-form-page .summary-card::before {
		content: ""; position: absolute; top: -20px; right: -20px; width: 100px; height: 100px;
		background: linear-gradient(135deg, var(--ztc-bg-bg-9, #2E4DFD) 0%, var(--ztc-bg-bg-8, #002466) 100%);
		border-radius: 50%; opacity: 0.12;
	}
	.admin-user-form-page .summary-title { font-size: 1.2rem; font-weight: 700; color: var(--ztc-text-text-2, #1A1719); margin-bottom: 0.5rem; }
	.admin-user-form-page .summary-date { font-size: 0.875rem; color: var(--ztc-text-text-3, #544E51); }
	.admin-user-form-page .section-card {
		background: var(--ztc-bg-bg-1, #fff); border-radius: 12px;
		box-shadow: 0 2px 8px rgba(0,0,0,.06); padding: 1.5rem; margin-bottom: 1.25rem;
		border: 1px solid rgba(0,0,0,.04); transition: box-shadow 0.3s ease, border-color 0.3s ease;
	}
	.admin-user-form-page .section-card:hover { box-shadow: 0 4px 12px rgba(0,0,0,.08); }
	.admin-user-form-page .section-card h3 {
		font-size: 1.1rem; font-weight: 700; color: var(--ztc-text-text-2, #1A1719); margin-bottom: 1rem;
	}
	.admin-user-form-page .form-control, .admin-user-form-page .form-select {
		border-radius: 8px; border-color: rgba(0,0,0,.12); transition: border-color 0.3s ease, box-shadow 0.3s ease;
	}
	.admin-user-form-page .form-control:focus, .admin-user-form-page .form-select:focus {
		border-color: var(--ztc-bg-bg-9, #2E4DFD); box-shadow: 0 0 0 3px rgba(46,77,253,0.15); outline: none;
	}
	.admin-user-form-page .form-control.is-invalid { border-color: #dc3545; }
	.admin-user-form-page .invalid-feedback { color: #dc3545; font-size: 0.85rem; }
	.admin-user-form-page .hint { font-size: 0.8125rem; color: var(--ztc-text-text-3, #544E51); margin-bottom: 0.5rem; }
	.admin-form-sticky-save {
		position: fixed; bottom: 1.5rem; right: 1.5rem; z-index: 100;
	}
	.admin-form-sticky-save .save-btn { box-shadow: 0 4px 14px rgba(46,77,253,0.35); }
	.admin-user-form-page .save-btn {
		font-family: var(--ztc-family-font1, inherit) !important; font-size: var(--ztc-font-size-font-s20, 1rem) !important; font-weight: 700 !important;
		text-transform: uppercase !important; letter-spacing: 0.02em;
		padding: 16px 28px !important; border-radius: 8px !important; border: none !important;
		background: var(--ztc-bg-bg-9, #2E4DFD) !important; color: var(--ztc-text-text-1, #fff) !important;
		position: relative !important; overflow: hidden !important; z-index: 1; transition: color 0.4s ease;
		box-shadow: none !important;
	}
	.admin-user-form-page .save-btn::after {
		content: "" !important; position: absolute; left: 50%; top: 0; width: 1px; height: 100%;
		background: var(--ztc-bg-bg-8, #002466) !important; z-index: -1; border-radius: 8px;
		transition: width 0.4s ease, height 0.4s ease, left 0.4s ease, top 0.4s ease, opacity 0.4s ease;
		visibility: hidden; opacity: 0;
	}
	.admin-user-form-page .save-btn:hover:not(:disabled) { color: #fff !important; background: var(--ztc-bg-bg-9, #2E4DFD) !important; transform: none !important; }
	.admin-user-form-page .save-btn:hover:not(:disabled)::after { width: 100%; height: 100%; left: 0; top: 0; visibility: visible; opacity: 1; }
	.admin-user-form-page .btn-outline-secondary {
		border-radius: 8px; border-color: rgba(0,0,0,.15); color: var(--ztc-text-text-2, #1A1719);
	}
	.admin-user-form-page .btn-outline-secondary:hover { border-color: var(--ztc-bg-bg-9, #2E4DFD); color: var(--ztc-bg-bg-9, #2E4DFD); background: rgba(46,77,253,0.04); }
	.admin-user-form-page .form-check-input:checked { background-color: var(--ztc-bg-bg-9, #2E4DFD); border-color: var(--ztc-bg-bg-9, #2E4DFD); }
	.admin-user-form-page .form-check-label { font-weight: 500; color: var(--ztc-text-text-2, #1A1719); }
	.admin-user-form-page .roles-group .form-check { margin-bottom: 0.5rem; }
</style>
